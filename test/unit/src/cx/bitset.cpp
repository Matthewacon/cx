#include <cx/test/common/common.h>
#include <cx/bitset.h>

namespace CX {
 TEST(Bitset, test) {
  /*
  constexpr auto const nBits = 2;
  Bitset<nBits> b;
  b & (uint32_t)3;
  printf("b: 0b");
  for (SizeType i = 0; i < nBits; i++) {
   printf("%d", b[i] ? 1 : 0);
  }
  printf("\n");
  */

  /*
  Bitset b{0x3};
  printf("%s\n", typeid(b).name());
  */
 }
}
